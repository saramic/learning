import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'saramic.ionic.recipe',
  appName: 'ionic-angular-course',
  webDir: 'www'
};

export default config;
