{
  "name": "to-do",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "cypress": "cypress open --component --browser chrome",
    "e2e": "cypress open --e2e --browser chrome",
    "full-build": "vitest --run && cypress run --component && cypress run --e2e"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.0.1",
    "@vitest/coverage-c8": "^0.22.0",
    "cypress": "^10.6.0",
    "vitest": "^0.22.0"
  }
}
